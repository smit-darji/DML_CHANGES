<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>snowflake-liquibase</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <properties>
        <liquibase.version>4.2.2</liquibase.version>
        <liquibase.snowflake.version>4.2.1</liquibase.snowflake.version>
        <snowflake.jdbc.version>3.22.0</snowflake.jdbc.version>
        <snowflake.envId>${env.SNOWFLAKE_ENV_ID}</snowflake.envId>
    </properties>

    <modules>
        <module>RZ/ACCOUNTING_ANALYTICS</module>
        <module>RZ/AGILE_REPORTING</module>
    </modules>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.liquibase</groupId>
                    <artifactId>liquibase-maven-plugin</artifactId>
                    <version>${liquibase.version}</version>
                    <configuration>
                        <changeLogFile>changelog.xml</changeLogFile>
                        <driver>net.snowflake.client.jdbc.SnowflakeDriver</driver>
                        <username>${env.SNOWFLAKE_USER}</username>
                        <password>${env.SNOWFLAKE_PASSWORD}</password>
                        <url>${snowflake.urlBase}</url>
                        <verbose>true</verbose>
                        <promptOnNonLocalDatabase>false</promptOnNonLocalDatabase>
                        <expressionVariables>
                            <envId>${snowflake.envId}</envId>
                        </expressionVariables>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>org.liquibase.ext</groupId>
                            <artifactId>liquibase-snowflake</artifactId>
                            <version>${liquibase.snowflake.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>net.snowflake</groupId>
                            <artifactId>snowflake-jdbc</artifactId>
                            <version>${snowflake.jdbc.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>dev</id>
            <activation>
                <property>
                    <name>env.SNOWFLAKE_ENV_ID</name>
                    <value>DEV</value>
                </property>
            </activation>
            <properties>
                <snowflake.urlBase>jdbc:snowflake://${env.SNOWFLAKE_ACCOUNT}.snowflakecomputing.com/?warehouse=${env.SNOWFLAKE_WAREHOUSE}&amp;role=${env.SNOWFLAKE_ROLE}&amp;private_key_file=snowflake_rsa_key.p8</snowflake.urlBase>
            </properties>
        </profile>

       

        <profile>
            <id>default</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <snowflake.urlBase>jdbc:snowflake://${env.SNOWFLAKE_ACCOUNT}.snowflakecomputing.com/?warehouse=${env.SNOWFLAKE_WAREHOUSE}&amp;role=${env.SNOWFLAKE_ROLE}&amp;private_key_file=snowflake_rsa_key.p8</snowflake.urlBase>
            </properties>
        </profile>
    </profiles>

</project>
